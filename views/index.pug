extends layout

block content
  h1= title
  p Welcome to the #{title} friend!
  ul
    each message in messages.length ? messages : ['There are no messages']
      li
        h2= message.text
        h3= message.user 
        h4= message.added
